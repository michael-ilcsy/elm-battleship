{
  "scripts": {
    "start": "run-p start:elm watch:sass",
    "start:elm": "elm-app start",
    "install:elm": "elm-app install",
    "build": "run-s build:sass build:elm",
    "build:elm": "elm-app build",
    "build:sass": "sass ./src:./src",
    "watch:sass": "yarn build:sass --watch",
    "test": "elm-app test",
    "test:watch": "elm-app test --watch"
  },
  "dependencies": {
    "create-elm-app": "^4.2.26",
    "npm-run-all": "^4.1.5",
    "sass": "^1.26.10"
  }
}
